alfred pacicco
/I created a new vim .vimrc directory and placed inside the directory a list of commands used to alter my directory to my liking. I then saved this list, confirmed it saved with the cat .vimrc function, and then placed in my home directory while renaming it. 

syntax on
set ruler
set autoindent
set number
set ignorecase
color elflord

